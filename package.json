{
  "name": "{{ appName }}",
  "version": "{{ appVersion }}",
  "description": "{{ appDescription }}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "{{ appAuthor }}",
  "license": "ISC"
}
